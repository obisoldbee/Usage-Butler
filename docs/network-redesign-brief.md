# 网络 v2 原生交付状态

更新：2026-09-22 · 开发预览版 0.3.2（5）。[原始整改说明](archive/2026-09-20-network-redesign-brief.md)按原字节归档，仅作历史输入。

## 采纳的 Mac 范围

原生 SwiftUI/macOS 13；保留额度、内存、设置。下载包是设计参考，网页原型及其测试不是原生运行证明。Windows 的内存 commit 指标和合仓建议未迁入 Mac。设置仍即时保存，飞书检查改为用户显式发起且旧配置结果失效。

|整改项|当前实现|验证边界|
|---|---|---|
|R1 独立纵轴|上传红色在上、下载蓝色在下；共享时间/游标，独立零基线/nice刻度，单调时钟防抖|纯逻辑测试与 540pt 原生窗口；VoiceOver朗读、全部显示缩放尚未验收|
|R2 五档范围|五档独占整行，每项32pt高且整块命中；默认1小时|0.3.2 原生坐标点击未选中项空白边缘，五档通过|
|R3 历史缺口|采集侧保留最多2小时；局部声明cadence，不用窗口中位数；先分段后抽稀，重复发布不加点|混合5s/1s、3s/20s缺口、epoch/session、单向缺失、wall rollback测试|
|R4 累计起点|各向当前可验证段各有bytes/since；reset、缺失或长静默重新基线|新合同及v1读取迁移测试；不再声称跨epoch总量完整|
|R5 来源与健康|0.3.1 改为公开 IFMIB_IFALLDATA / IFDATA_GENERAL；所选范围与当前健康分开|NET_RT_IFLIST2 被真机 >4 GiB 读数对账证伪；新实际 reader 必须通过系统读数前后夹取测试|
|R6 生命周期|stop可重新start；拒绝旧session心跳；沿用有界正常退出|离线生命周期测试与本轮原生Quit；睡眠/唤醒长期浸泡待验|
|R7 应用完整交互|Debug显式合成应用、搜索/排序/关注、五详情、返回、草稿、脱敏预览与本地保存|实际原生UI走查；Release编译排除，无真实按应用/目标观察或防护|

## 运行演示

```bash
./script/build_and_run.sh --fixture-panel
```

只在 Debug 加载离线 fixture；独立验证窗口复用 `PanelRootView`。静态 fixture 会真实进入过期状态，点“刷新”重新载入样例，不能用它证明真实采集。可追加 `--network-v2-dark` 到 Debug 预览启动参数检查深色。正式版为菜单栏应用。

## 能力与后续门

Production 只有接口观察，不相加物理/TUN/回环，不猜Wi-Fi或代理软件归属。真实应用/连接归属、系统扩展授权、阻断、磁盘历史和网络提醒仍未实现，不是完整P0或防上传产品。Debug规则不会操作系统；导出是演示数据，不是网络取证结果。没有 HTTPS 解密、载荷采集、流量上传或自动发送消息的新能力。

数据细节见[原生技术合同](network-v2-contract.md)，评审入口见[review guide](network-v2-review-guide.md)。完整端到端验证收据保存在本地项目审查记录，公开记录只描述本次实际执行范围。

0.3.1 同时修正历史告警永久保留和网络页跳过 Tab 的问题；应用启动前的历史空白不告警。真实中断在所选范围内且有中断前观测时才提示，统计说明保留方向、起点、原因。修复及验证要求见技术合同和评审入口。

0.3.1 历史验证：781 项 XCTest 通过；定向实际读数测试在修复前失败、修复后通过。arm64 本机完成 4 GiB + 1 MiB 回环载荷传输，实际 reader 的上传/下载计数跨越 32 位边界仍单调增长（仅本机回环，无外部流量）；最终 Debug 原生窗口连续两轮 Tab 切换与输入框焦点例外通过。不能由此推导所有 macOS/VPN/睡眠拓扑或 x86_64 运行均通过。

0.3.2 修正五档时间按钮的命中区域：整块32pt高矩形可点击，包括文字周围空白。移除主面板“高级/收起”和“统计说明”折叠；“网络设置…”直接打开设置的网络页，集中采集开关、观察接口、用途解释与采样诊断。自动为推荐，手选仅用于排查特定接口，不改变网络连接。游标读数固定占位，网络概览不附加动画。

0.3.2 本轮验证：最终781项XCTest通过；旧版空白区域点击失败、新版五档空白边缘点击成功。Debug原生网络设置直达、手选隧道及恢复自动、统计说明静态展示、固定读数行与两轮Tab通过。Universal编译包含arm64/x86_64，运行走查仅当前arm64系统；未量化帧率、未执行macOS13 fallback/完整VoiceOver/全缩放矩阵。
